{
  "name": "hs-puzzle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "split:cards": "node scripts/split-cards.mjs",
    "predev": "bun run split:cards",
    "prebuild": "bun run split:cards",
    "dev": "rsbuild --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "deploy:cf": "pwsh scripts/deploy-cf.ps1 -Action deploy",
    "deploy:cf:guide": "pwsh scripts/deploy-cf.ps1 -Action guide"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@rsbuild/core": "^2.0.9",
    "@rsbuild/plugin-react": "^2.0.1",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "typescript": "^6.0.3"
  }
}
